import { Box } from "@mui/material";
import { PageHeader } from "../../components/PageHeader";

export function StudyPage() {
  return (
    <Box sx={{ mt: 10, ml: 20, mr: 20 }}>
      <PageHeader title={"Study"} />
    </Box>
  );
}
